all:
  vars:
    ansible_connection: ssh
    ansible_user: root
    ansible_ssh_private_key_file: ${digitalocean_private_key}
  hosts:
    ${codeserver_name}:
      ansible_host: ${ansible_host}
      codeserver_domain: ${codeserver_domain}
      codeserver_password: ${codeserver_password}